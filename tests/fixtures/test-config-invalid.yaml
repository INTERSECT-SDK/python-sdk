status_interval: 45
id_counter_init: 125.3

hierarchy:
  service: {}
  subsystem: false
  system: 123
  facility: []

broker:
  username: ""
  password: null
  host: devnull.ornl.gov
  port: -1883

# all of these are invalid, try using the load-from-file function's callback param to remove items (not everything prints out)
argument_schema:
  type: "red"
  properties: 9001
  description: ["never", "gonna", "give", "you", "up"]
